.container
  .col-md-12
    \ Rows:
    = select_tag "rows", options_for_select(1..20), id: 'rows'
    \ Columns:
    = select_tag "columns", options_for_select(1..40), id: 'columns'
    = submit_tag "submit", id: 'submit_details'


    .layout-row
      #layout
